(ns carabiner.cobblestone.core)

(defn load-cobblestone-file [load-data]
  ;; todo
  )

(defn compress-data [spritely-data]
  ;; todo
  )

(defn build-save-file [base64]
  ;; todo
  )

(defn build-image [bas64]
  ;; todo
  )

(defn build-tile-image [base64]
  ;; todo
  )

(defn build-map-image [base64]
  ;; todo
  )

(defn build-printable [base64]
  ;; todo
  )
