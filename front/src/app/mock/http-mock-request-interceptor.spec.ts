import { HttpMockRequestInterceptor } from './http-mock-request-interceptor';

describe('HttpMockRequestInterceptor', () => {
  it('should create an instance', () => {
    expect(new HttpMockRequestInterceptor()).toBeTruthy();
  });
});
