import { DatasetMetaPartSet } from './dataset-meta-part-set';

describe('DatasetMetaPartSet', () => {
  it('should create an instance', () => {
    expect(new DatasetMetaPartSet()).toBeTruthy();
  });
});
